name: 更新福彩3D数据并发布

on:
  schedule:
    # 北京时间22:17至次日02:17每小时重试一次，08:17再补一次。
    - cron: "17 14-18 * * *"
    - cron: "17 0 * * *"
  workflow_dispatch:
  push:
    branches: ["main"]

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: 读取官方开奖并生成静态数据
        env:
          REQUIRE_OFFICIAL_REFRESH: "1"
        run: node scripts/generate-v7-static-data.mjs
      - name: 保存验证后的最新开奖
        run: |
          if ! git diff --quiet -- scripts/data/fc3d-full-history.json scripts/results/full-history-integrity.json; then
            git config user.name "fc3d-data-bot"
            git config user.email "actions@users.noreply.github.com"
            git add scripts/data/fc3d-full-history.json scripts/results/full-history-integrity.json
            git commit -m "Update verified FC3D draw data [skip ci]"
            git push origin HEAD:main
          fi
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: pages

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: 发布到 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
